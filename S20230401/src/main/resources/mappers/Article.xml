<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java501.S20230401.ArticleMapper">

	<select id="noticeTotal" resultType="int">
	<![CDATA[
		select Count(*) FROM article WHERE brd_id >=1500 and brd_id <1511
	]]>
	</select>
	
	<select id="shArticleListNotice" parameterType="Article" resultType="Article">
	 <![CDATA[
		SELECT *
		FROM
		(
   			SELECT rownum rn, a.*
    		FROM
        			(SELECT * 
        			 FROM article join member using(mem_id) 
        			 WHERE brd_id >=1500 and brd_id <1511 order by art_id desc) a
		)
		WHERE rn BETWEEN #{start} and #{end}
 	 ]]>	
	</select>
	
	<select id="shNoticeSelOne" parameterType="Article" resultType="Article">
	  <![CDATA[
        SELECT *
        FROM
	        (SELECT *
	        FROM article join member using(mem_id)
	        WHERE brd_id >=1500 and brd_id <1511)
	    WHERE art_id = #{art_id}
	  ]]>
	</select>
	
    
	
</mapper>